# S1 合同审计 — contract-l4-element

> **日期**：2026-05-19  
> **基线**：`main`（#15 合并前审计，供 #14 评审互参）  
> **审计人**：Agent（`design.md` §2）

| # | 检查项 | 结果 | 证据 |
|---|--------|------|------|
| S1-1 | `PH_Element_Compute_Ke_Arg` ↔ L5 填充 | **通过** | `PH_Elem_Def.f90` L331–341：`elem_idx`, `l3_elem_idx`, `mat_pt_idx`, `nDof`, `mat_props_in`, `evo%Ke`, `status`；`RT_Asm_Solv.f90` L1875–1889 逐项赋值 + `RT_Asm_Solv_KeArg_AttachMatProps` |
| S1-2 | `mat_pt_idx` / slot 与 Material R2 | **通过** | `PH_Elem_MaterialRoute.f90`：`slot_pool(mat_pt_idx)`；Material `CONTRACT.md` R2 与 Element §2 材料路由一致 |
| S1-3 | Populate `elem_type_cache` ↔ Reg | **通过** | Element `CONTRACT.md` L122：未注册仅 `log_warn`，Populate 保持 OK；`PH_Elem_Reg_IsRegistered` |
| S1-4 | `load_magn_in` vs `F_ext` | **通过** | Element `CONTRACT.md` L157 + Assembly §5.4 交叉引用；与 PR01 一致 |
| S1-5 | Legacy `PH_Elem_Contm` / G6 路径 | **通过（登记债）** | `CONTRACT.md` §2.1 **legacy**；G6 **红** 已记入 `P2_ELEMENT_GAP_SNAPSHOT.md`，scope 限于 `p2-element-legacy-contm-retire` |

**结论**：S1 五项均可勾选；**不阻塞** #14（Material W2）合并。P2 实现波次仍受 G3–G6 约束。
