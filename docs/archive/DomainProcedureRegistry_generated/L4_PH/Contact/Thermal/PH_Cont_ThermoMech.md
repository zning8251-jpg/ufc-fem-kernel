# `PH_Cont_ThermoMech.f90`

- **Source**: `L4_PH/Contact/Thermal/PH_Cont_ThermoMech.f90`
- **Generated (UTC)**: 2026-05-07T07:47:17Z
- **MODULE (heuristic)**: `PH_Cont_ThermoMech`

> Heuristic scan: verify critical files against compiler view; nested TYPE / continuations may mis-classify.

## 命名 — 三段式 / 四段式（对照规范）

与 [CONVENTIONS.md](../../../../CONVENTIONS.md) §1.1–§1.2、[UFC_命名与数据结构规范.md](../../../../../UFC_命名与数据结构规范.md) §3 一致（以下为 **按 `.f90` 文件名 stem 的启发式**，非编译器语义）：

- **stem**: `PH_Cont_ThermoMech`
- **逻辑主线（默认三段式 `PH_{Domain+Feature}`）**: `PH_Cont_ThermoMech`
- **第四段角色**: *(未解析到闭集内后缀 — 可能为纯三段式主线，或非标准 stem；以源码与合同为准)*
- **源码子路径（层下目录，不含文件名）**: `Contact/Thermal`
- **Registry 布局（镜像 `ufc_core` 相对路径 + `.md`）**: `generated/L4_PH/Contact/Thermal/PH_Cont_ThermoMech.md` — *与 [`UFC_ufc_core_目录权威分类.md`](../../../../../05_Project_Planning/PPLAN/06_核心架构/UFC_ufc_core_目录权威分类.md) 物理树一致；三段式/四段式解析见上*

## TYPE blocks

### `PH_ContTH_Pair` (lines 42–53)

```fortran
  TYPE :: PH_ContTH_Pair
    ! Thermo-mechanical contact pair
    INTEGER(i4) :: pair_id
    REAL(wp) :: mechanical_gap      ! Mechanical gap
    REAL(wp) :: mechanical_pressure ! Contact pressure
    REAL(wp) :: temp_slave          ! Temperature on slave side
    REAL(wp) :: temp_master         ! Temperature on master side
    REAL(wp) :: heat_flux           ! Heat flux across interface
    REAL(wp) :: conductance         ! Thermal contact conductance
    REAL(wp) :: friction_heat       ! Heat generated by friction
    LOGICAL :: is_active            ! Active contact flag
  END TYPE PH_ContTH_Pair
```

### `PH_ContTH_ConductanceLaw` (lines 55–64)

```fortran
  TYPE :: PH_ContTH_ConductanceLaw
    ! Thermal contact conductance model
    INTEGER(i4) :: law_type    ! 1=Linear, 2=Power, 3=Exponential
    REAL(wp) :: param_a        ! Model parameter A
    REAL(wp) :: param_b        ! Model parameter B
    REAL(wp) :: param_c        ! Model parameter C
    CHARACTER(LEN=32) :: name  ! Model name
  CONTAINS
    PROCEDURE :: Evaluate => ConductanceLaw_Evaluate
  END TYPE PH_ContTH_ConductanceLaw
```

### `PH_ContTH_FrictionHeat` (lines 66–73)

```fortran
  TYPE :: PH_ContTH_FrictionHeat
    ! Frictional heating parameters
    REAL(wp) :: friction_coeff     ! Friction coefficient μ
    REAL(wp) :: slip_velocity(3)   ! Relative slip velocity
    REAL(wp) :: heat_partition     ! Heat partition factor [0,1]
    REAL(wp) :: flash_temp         ! Flash temperature rise
    LOGICAL :: use_flash           ! Enable flash temperature calculation
  END TYPE PH_ContTH_FrictionHeat
```

## Module-level procedures (`SUBROUTINE` / `FUNCTION`)

| Kind | Name | Line | Signature (first line) |
|------|------|------|-------------------------|
| SUBROUTINE | `PH_ContTH_ComputeHeatFlux` | 81 | `SUBROUTINE PH_ContTH_ComputeHeatFlux(pair, status)` |
| FUNCTION | `ConductanceLaw_Evaluate` | 109 | `FUNCTION ConductanceLaw_Evaluate(this, pressure, gap, temp_avg) RESULT(h_c)` |
| SUBROUTINE | `PH_ContTH_ComputeConductance` | 130 | `SUBROUTINE PH_ContTH_ComputeConductance(pair, law, roughness, status)` |
| SUBROUTINE | `PH_ContTH_ComputeFrictionHeat` | 160 | `SUBROUTINE PH_ContTH_ComputeFrictionHeat(pair, friction, normal_force, status)` |
| SUBROUTINE | `PH_ContTH_CoupledResponse` | 194 | `SUBROUTINE PH_ContTH_CoupledResponse(pairs, n_pairs, law, friction, &` |
| SUBROUTINE | `PH_ContTH_UpdateTemperature` | 251 | `SUBROUTINE PH_ContTH_UpdateTemperature(pair, dt, capacity_slave, capacity_master, status)` |
| SUBROUTINE | `PH_ContTH_Compute_ContactHeatFlux` | 283 | `SUBROUTINE PH_ContTH_Compute_ContactHeatFlux(gap_n, T_slave, T_master, &` |

## Procedures detected inside TYPE bodies

*(none — type-bound bodies often use `PROCEDURE ::` only; see TYPE blocks above)*

## INTERFACE blocks (outline)

*(none)*
