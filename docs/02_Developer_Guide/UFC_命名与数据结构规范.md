# UFC 命名与数据结构规范
> **文本修复（2026-04-25）**：部分汉字曾被批量编辑替换为 ?；**归档与 REPORTS 链接**已按仓库结构修正。正文请以 REPORTS 命名套件为准人工对照消缺。

> **合并?*：UFC_命名契约_四场景规?+ UFC_L3L4L5_约定俗成简写规范?> **用?*：类?模块/变量/子程序四场景契约 + L3/L4/L5 词根简写表。层级前缀、域级缩写、压缩词根以 [00-命名规范标准](archive_20260418/六层架构拆分_归档/00-总纲/00-命名规范标准.md)、[00-域级划分规范](archive_20260418/六层架构拆分_归档/00-总纲/00-域级划分规范.md) 为准?> **关联**：逐层逐域·全局-局部命名与验收标准?[UFC_命名规范与接口标准 v2.0（REPORTS）](../REPORTS/UFC_命名规范与接口标准_v2.0.md)。子程序内注释与计算?逻辑链流程图?[UFC_流程图与注释规范_英文Unicode](UFC_流程图与注释规范_英文Unicode.md)? 
> **Registry 对齐**：模块「主?+ 条件第四?+ 防碎化」工作流?L2 例外?[DomainProcedureRegistry/CONVENTIONS.md](../03_Domain_Pillars/DomainProcedureRegistry/CONVENTIONS.md) §1.1?.3?*MODULE 角色后缀闭集以本?§3.2 为权?*（合并旧白名单与 Registry 提议）?
---

## 一、总则与命名级?
### 1.1 三级与四级命?
- **三级命名（默认）**：`层级_域级_功能集`。对应目录：`Lx_XX/<Domain>/`，单层子目录下为功能集模块（?`MD_Mat_ElasticCore.f90`）。层级：IF_|NM_|MD_|PH_|RT_|AP_；域级：与子文件夹一致（见[域级划分标准](#域级划分标准与职责边?）；功能集：驼峰法压缩（PascalCase），?ElasticCore、LinSolv。功能集压缩必须遵循有限?工程常用缩写，不得引入语义混淆的极端缩写（详见第六章简写表）?- **四级命名（扩充）**：当某域下存?*次级子目?子文件夹**时，命名可扩充为 `层级_域级_功能集主模块_次级子模块`，对应路?`Lx_XX/<Domain>/<FunctionSet>/<SubModule>.f90`。例如：
  - `L4_PH/Mat/Damage/` ?模块?`PH_Mat_Dmg_Defn`（功能集主模?Dmg，次级子模块 Defn）；
  - `L4_PH/Elem/SLD3D/` ?模块?`PH_Elem_Sld3D_C3D8Defn`（功能集 Sld3D，次?C3D8Defn）?- **级数上限**：最?**4 ?*（层?域级-功能?次级子模块）。不得再扩展第五段，以免与“功能集驼峰压缩”和目录可读性冲突?- **四级次级子模?*：第四段仅允许少?*稳定角色?*，不得再拼业务语义。白名单：`Defn` | `Core` | `Eval` | `API` | `Brg` | `Mgr` | `Type` | `Reg`（与场景 B 角色后缀一致），避免与功能集驼峰语义重叠?- **功能集命名正?*（驼?+ 可读）：`ElasticCore`、`PlasticEval`、`StepCtrl`、`ContactBrg`、`LinSolv`?- **功能集命名反?*（禁止）：`EC`/`PL`/`SC` 等无上下文即无法理解的缩写；?`Stress` 压成 `S`、`Strain` 压成 `E`（易与能?模量混淆）?- **四段式（历史/兼容?*：`层级_域级_功能_后缀` 仍可用于类型/子程序等；类型第四段为四型（_Desc/_State/_Algo/_Ctx），**模块**角色?§3.2?*推荐 `_Ops`** 表示过程主体?*?*新起 `MODULE …_Algo` ?TYPE `_Algo` 同形），子程序为动作（Init/Eval/Add 等）。与三级/四级命名冲突时，**模块与文件名**以三级或四级为准?- **适用**：新?修改?Fortran 模块、类型、变量、子程序及自动生成代?*必须**符合本规范；冲突时以本文为准?
### 1.2 域级划分标准与职责边?
- **域级划分标准**（三选一或组合，?[00-域级划分规范](archive_20260418/六层架构拆分_归档/00-总纲/00-域级划分规范.md) 第七章为准）?  1. **功能集合**：按 ABAQUS 手册功能大类（几?材料/截面/载荷BC/约束/接触/?输出等）归为同一域；
  2. **数据归属与生命周?*：谁拥有该数据的定义、谁在运行时持有状态——定义归?L3_MD，本?单元公式归属 L4_PH，步/增量/装配归属 L5_RT?  3. **单一职责与无重叠**：同一层级内域级名称唯一，职责不交叉；跨层同名域（如 Mat）通过 Brg/Ctx 桥接，不复制实现?- **域级与功能模组的职责边界**?  - **域级** = 该层下的一级子文件夹（?Mat、Elem、Solv、Step、Inp），代表“功能域”边界；
  - **功能模组** = 域内模块/文件或二级子目录下的模块集合，代表“功能集”或“功能集+次级子模块”的实现边界?  - **10 ?ABAQUS 核心域名（禁止缩写）**：Model、Part、Assembly、Mesh、Section、Material、Amplitude、LoadBC、Interaction、Step；域级目录名与模块前缀须与上述关键字对齐，不得自造缩写；
  - **边界判定**：若某能力属于“模型定?注册/查询”，归该层域?Core/Mgr/API；若属于“数?物理计算”，归该层或下层域级 Eval/Core；若属于“跨层适配”，?Brg。详?00-域级划分规范 7.2?.3?.4?- **重命名与架构图顺?*：只有先确定**层级-域级-功能集（及必要时次级子模块）**架构图与目录表后，才在功能模块级别执行重命名；不得在域级未统一前对模块做大规模重命名?
### 1.3 三层精简命名的可行性与优先级（结论摘要?
- **三层精简命名可行**：`层级_域级_功能集` 作为 **ufc_core 内模?文件的主命名格式** 可行且推荐；原“层?域级-功能-后缀”仅保留?**TYPE 四型后缀 / 子程序动作后缀**，不再作为模块名主结构?- **功能集驼峰允许且推荐**：须基于**有限?工程惯用缩写**?*不得压缩到丢失物理含?*，避免领域专家误读（正例/反例?1.1）?- **命名?P0 横切任务**：命名不是“收尾工作”，而是**开发全程始终进行的检查项**；若实现与规范冲突，**先改命名，再?改逻辑**；通过 CI 集成 `check_naming_standard.py` 等防止“新代码倒回旧风格”?- **四级扩展**：仅当某域存在二级子目录时启用，次级子模块名限定?1.1 所列角色白名单（Defn/Core/Eval/API/Brg/Mgr 等），禁止在第四段继续拼业务语义?- **防止“新脏旧净?*：存量可分批迁移?*新增/修改代码**必须即刻采用三层精简命名与四?角色后缀，禁止引入新的历史遗留风格?- **有限?工程语义约束**：功能集与驼峰须遵循第六章简写表，不得将应力/应变/刚度等压成单字母歧义缩写?
### 1.4 主线过长：功能段内下划线切分；MODULE 勿用 `_Algo` 充当「过程后缀?
- **层级前缀已固?*（`IF_` / `NM_` / `MD_` / `PH_` / `RT_` / `AP_`）后?*不得**再用 **`MODULE …_Algo` / `*_Algo.f90`** 表示「过程体编译单元」——过程体 **一?* ?**`…_Ops`**（或 §3.2 闭集中其?**模块角色** 后缀），**`_Algo` 仅保留给四型中的「算法描?TYPE?*（见下节表与 §3.2 辨析）? 
- **中段过长**（`{Domain}_{Feature}` ?Feature 单团过长）：**优先**?**第六章简写表** 与域习惯缩写 **缩短词根**?*其次**允许?**同一 Feature 语义?* 把原 PascalCase **拆成若干下划线词**，仍算作 **第三?`Feature` 的内部形?*?*?*据此新增「第五层级」段名。例：`PH_Brg_Gauss_Pts3D_Ops` 优于无节制堆长驼峰；**禁止**为变长再?`Layer_Domain_Sub1_Sub2_Feature` 五段式主线? 
- **与路径四级关?*：若目录上已?**次级子模?*（?.1 路径四级），**模块?*仍须遵守 **级数上限**；能?**目录 + 短模块名** 表达的，**不要**把整条路径语义硬编码?`MODULE` 名?
---

## 二、场?A：TYPE（类型名?
| 四型              | 含义  | 后缀   |
| ----------------- | ----- | ------ |
| 模型描述/只读配置 | Desc  | _Desc  |
| 运行时状?       | State | _State |
| 算法描述?       | Algo  | _Algo?*?TYPE ?*?*MODULE** 过程体用 **`_Ops`**，见 §3.2?|
| 局部上下文/控制   | Ctx   | _Ctx   |

**格式**：`<层前缀>_<?_<功能可?_<四型后缀>`。禁止类型用 _Mgr/_Core?*标识符长度以可读与域 CONTRACT 为准**?*?2 字符为软约束**（超长时优先简写词根与 §1.4 功能段内切分，而非消去语义）。例外：顶层容器、抽象基类（_Base/_Interface）可无四型?
### 2.1 结构化过程参?TYPE：`*_In` / `*_Out` 对与「合?Bundle」（可选）

用于 **子程序形参打?* 的派生类型（**?*上表「四型」语义；**?*使用 `_Desc/_State/_Algo/_Ctx` 作为其第四段）时，允许两种等价契约写法，**由域 CONTRACT 选定其一并在域内统一**?
| 模式 | TYPE 命名 | 说明 |
|------|------------|------|
| **A · 分拆** | `…_In`、`…_Out`（或 `…_Arg_In` / `…_Arg_Out`?| In/Out **强分?*、构?销毁时机不同、或仅一侧跨层传递时更清晰?|
| **B · 合并（可选）** | **单一** `TYPE`，例?`PH_Brg_GetGaussPoints3D_Arg` 或域约定?`…_Params` / `…_Bundle` | 所有原 In/Out 分量置于 **同一** `TYPE` 内；**成员行尾** 用注?**`! [IN]`**?*`! [OUT]`**?*`! [INOUT]`** 标注数据向（?Principle #14 ?`[IN]`/`[OUT]` 叙述对齐，供人工审查与工具扫描）?*不再**强制 `_In` / `_Out` 两个类型名?|

**合并模式的命名与冲突**?
- **禁止**?**同模块内** 已有 **`SUBROUTINE`/`FUNCTION` 同名** ?`TYPE` 共用一个标识符（Fortran 易混淆且工具链不友好）。若过程名为 `PH_Brg_GetGaussPoints3D`，合并参?TYPE **?* 带区分后缀?*推荐** `_Arg`；亦可用 `_Params` / `_Bundle` 等域约定词，**仍非**四型后缀）? 
- **INTENT**：合并类型常?**一?* 形参传入；具?`INTENT(IN)`/`OUT`/`INOUT` ?**包装子程?* ?**接口?* 侧按?CONTRACT 固化?*不得**仅靠注释替代编译器可见的 `INTENT` 规则?**对外 API**? 
- **SIO**：与 Principle #14 ?`*_Arg` 命名可并存：合并 TYPE 可作?`*_Arg` ?**唯一** 派生类型载体，域内文档须说明「字段级 In/Out 以注?+ CONTRACT 为真源」? 
- **Registry**：`generated/*.md` 对两类写法均?TYPE 块摘录；**?*强制全局改名为合并模式?
---

## 三、场?B：Module 与文件名

### 3.0 与「路径四级」的关系（避免混淆）

- **§1.1 路径/模块四级（Defn、Core…）**：描?**目录次级子模?* ?**少数稳定短词**（`Defn` | `Core` | `Eval` | …），与 **物理/元数据切?* 对齐? 
- **§3.2 模块角色后缀闭集（_Def、_Ops…）**：描?**编译单元职责**（类型为?/ 过程为主 / 桥接 / …），用?**主线名上的第四段**（见 §3.1）? 
- 二?**不同?*：可出现「路径上四级 + 模块名上再带角色后缀」的个例，但?**避免语义重复**（例如功能集已叫 `…Core`，模块名不再?`_Core`，除非团队显式约定）?
### 3.1 模块主线名、超长拆分与防碎化（?Registry 一致）

- **默认（三段式逻辑名）**：`{Layer}_{Domain}_{Feature}`，例 `IF_Base_StructMeta`。`{Domain}_` ?**一个槽?*（子域缩写并在此处，不叠 `Layer_Domain_Subdomain_Feature` 四段语义）? 
- **允许加第四段（角色后缀?*：当 **?`.f90` 体量过大**（经验阈?**??000 ?*）或 **必须?`USE` ?/ 分离类型与过?* 时，在主线后追加 **§3.2 闭集?* 后缀?*过程主体模块推荐** `…_Ops`（例：`IF_Base_StructMeta_Ops`），?§3.2 ?TYPE **`…_Algo`** 的区分? 
- **防碎?*：禁止「一过程一文件」式过碎模块；长?**?<300?00 ?* 且无独立复用价值的切片，应 **并回** 相邻编译单元。详?[DomainProcedureRegistry/CONVENTIONS.md §1.3](../03_Domain_Pillars/DomainProcedureRegistry/CONVENTIONS.md)?
**字符长度**：历史规定「模块标??2 字符」为 **软约?*；超长主线优?**缩短 Feature 词根**（仍须可读），其次再拆文件，**?*靠无意义缩写硬塞?32 字符?
### 3.2 MODULE 角色后缀闭集（合并版 · 权威?
以下 **并集** 为当前仓库认可的 **MODULE / `.f90` 主文件名** 第四段（及少数专用第三段以后缀形式出现）的 **允许集合**?*闭集外新后缀**须走评审并同步更新本文、`../03_Domain_Pillars/DomainProcedureRegistry/CONVENTIONS.md` 与命?CI?
**?TYPE 四型区分**：类型名第四段仅?**`_Desc` / `_State` / `_Algo` / `_Ctx`**（见 §二）——其?**`_Algo` 仅表示「算法描?TYPE?*?*模块**上的过程主体 **推荐 `_Ops`**?*避免**再使用与四型同字?**`…_Algo` 作为 `MODULE` ?*（存量除外，见下表「遗留」行）?
| 后缀 | 语义?| 典型内容 | 与易混项辨析 |
|------|--------|----------|----------------|
| **_Def** | 定义/契约 | 域类型、常量、轻 TBP；L3 模型真相倾向 | **?* 类型?`_Desc`；模?`_Def` 可含多个 `*_Desc` TYPE |
| **_Types** | 类型聚合 | ?`PUBLIC` TYPE/枚举为主、极少过?| 新代码优先；存量 **`_Type`** 视为同角色别名，迁移时统一?`_Types` |
| **_Ops** | **运算 / 过程主体（MODULE 推荐?* | 模块过程、数值主路径、SIO 以外的纯过程?| **?TYPE `*_Algo` 解?*；与 **`_Eval`**：`_Eval` 偏本??**求?*；与 **`_Proc`**：同一 `Feature` 主线 **二选一**，勿 `_Ops`+`_Proc` 双拆除非仍过?|
| **_Algo** | **（仅 MODULE · 遗留?* | 存量 `*_*_Algo.f90` / `MODULE …_Algo` | **新代码勿新增**；迁移时改名 **`…_Ops`**?*TYPE** `…_Algo` 仍为四型之一（§二），二?**不同对象** |
| **_Proc** | 过程集（备选） | 仅当 **`_Ops`** 单文件仍过长时的第二过程单元 | 见上?*?*?`_Core` 同时表示「同一拆分目的?|
| **_Core** | 内核 | L4 单元/材料?**热路径混?*（类?过程?| 域习惯保?`_Core` 时不动；**新拆文件**优先 **`_Ops`** / `_Eval` |
| **_Eval** | 求?| Gauss 点、本?场变量求?| ?**`_Ops`** 分工见上 |
| **_Solv** | 求解 | ?非线性求解器入口?| |
| **_Integ** | 积分/时间积分 | TMF、时间步、积分规?| |
| **_Assem** | 组装 | 刚度/残差组装、CSR ?| |
| **_Apply** | 施加/SIO | `Apply_*`、SIO 编排?| |
| **_Brg** | 桥接 | 层间适配、UF/RT ?| ?**`_API`**：`Brg` 偏适配；`API` ?**稳定对外契约** |
| **_API** | 对外 API | 稳定导出边界 | 见上 |
| **_Idx** | 索引/全局?| `g_ufc_global` ?Idx 路径 | |
| **_Sync** | 同步 | Legacy→域、镜像同?| |
| **_Mgr** | 管理/编排 | 容器、注册调?| ?**`_Ctrl`**：`Mgr` 偏资源聚合；`Ctrl` 偏步?流程控制 |
| **_Ctrl** | 控制 | 状态机、驱?| 见上 |
| **_Parse** | 解析 | 关键字、文本、INP 片段 | |
| **_Valid** | 校验 | 静?结构校验 | |
| **_Reg** | 注册?| 符号、场变量注册 | |
| **_Defn** | 定义块（L4?| 单元 Defn 元数据切?| ?**`_Def`**：`Defn` ?**单元族定义块**；`Def` ?**域级定义模块** |
| **_Init** | 初始化族 | 生命周期集中?*少用?MODULE ?*?| 优先 **`SubName_Init`** 过程名；模块?`_Init` 仅在有明确单一职责时使?|
| **_Util** | 工具 | 无状态辅助、数学小工具 | |
| **_Ref** | 参?适配 | 参照实现、实验分?| **可?*；不进主构建时可剔除 |
| **_Test** | 测试 | 测试树专用桩 | **可?* |
| **_Demo** | 示例 | 演示 | **可?* |

**选用策略（避免「分类看起来全但不知道怎么选」）**?
1. **先定主线** `{Layer}_{Domain}_{Feature}`，再决定是否需要第四段? 
2. **先按职责选类**：定义类 ?`_Def` / `_Types`；计算类 ?**`_Ops`** / `_Eval` / `_Core`；组装求??`_Assem` / `_Solv` / `_Integ`；跨??`_Brg` / `_API`；全局 Idx ?`_Idx`；Legacy 同步 ?`_Sync`? 
3. **同一主线最多一个「主过程文件?*：默?**`_Ops`**；仅当过大再?**`_Proc`** 或改?**`_Eval`** ?**不同语义** 的第二文件? 
4. **存量不动、增量从?*：旧模块后缀与上表不完全一致时?*迁移 PR** 再收敛，避免与功能改动混提交?
**格式（与 §1.1 衔接?*?
- **三级**：`<层前缀>_<?_<功能?`，如 `MD_Mat_ElasticCore`、`PH_Elem_C3D8Core`。文件名与模块名一致（`.f90`）? 
- **路径四级**（仅当该域下存在次级子目录时）：`<层前缀>_<?_<功能集主模块>_<次级子模?`，如 `PH_Mat_Dmg_Defn`；次级子模块词仍?**§1.1 白名?*（`Defn` | `Core` | `Eval` | …）? 
- **模块?+ 角色第四?*：`<层前缀>_<?_<功能>_<角色后缀>`，其?**`<角色后缀>` ?§3.2 闭集**；与路径四级 **可组合但忌语义重?*（?.0）?
---

## 四、场?C：变量名

- 单实例建议与类型四型对应?_desc | *_state | *_algo | *_ctx。集合可用复数或 _list/_vec?- 禁止超过 20 字符。Context 变量不得?_state 结尾；State 不得?_ctx 结尾?
---

## 五、场?D：子程序/过程?
**动作白名?*：Init | Build | Eval | Compute | Form | Get | Set | Update | Valid | Parse | Clean | Destroy | Add | Remove | Register | Apply | Assemble?
- **对外导出**：必须四段式全名（层_域_功能_后缀）?- **模块内实?*：允许短名（功能_后缀 ?功能_细节_后缀），导出须以全名或别名提供?- 禁止纯名词无动词；禁止超?28 字符?
---

## 六、约定俗成简写表（L3/L4/L5?
| 全称/长写     | 简?        | 适用                     |
| ------------- | ------------ | ------------------------ |
| Properties    | Props        | 成员、形?              |
| UserElement   | UEL          | 类型/模块?             |
| Dimension     | Dim          | 成员、形?              |
| constitutive  | const        | 成员、前缀               |
| temperature   | temp         | 成员、形?              |
| material      | mat / Mat    | 成员、形?              |
| coordinate(s) | coord        | 成员、形?              |
| number/num    | n ?num     | 前缀（numNodes→nNodes?|
| formulation   | Formul       | 类型/形参                |
| context       | Ctx          | 类型/形参                |
| definition    | def          | 后缀                     |
| configuration | cfg          | 后缀                     |
| parameter(s)  | param/params | 成员、形?              |
| coefficient   | coef         | 成员、形?              |

**适用范围**：L3_MD、L4_PH、L5_RT 内成员名、形参、局部变量；新代码一律采用，存量改到再换。对?API/INP 关键字不简写?*层级前缀、角色后缀、四型后缀**仍按 00-命名规范标准?0-域级划分规范执行?
---

## 七、禁止总表

| 场景   | 禁止?                                                              |
| ------ | -------------------------------------------------------------------- |
| TYPE   | 四型外自造后缀；类型用 _Mgr/_Core?*长度优先可读**（≤32 为软约束，见 §二表下说明） |
| Module | 以四型结尾；角色白名单外后缀?32 字符                               |
| 变量   | 与类型语义脱节；Context ?_state 结尾、State ?_ctx 结尾?20 字符 |
| 子程?| 纯名词无动词；动作白名单外；>28 字符；对外仅短名                     |
| 通用   | 无层级前缀；域级自造缩写；未压缩长词根                               |

---

## 八、四场景汇?
| 场景      | 对象   | ?4 ?                                             |
| --------- | ------ | ---------------------------------------------------- |
| A. 类型   | TYPE   | _Desc\| _State \| _Algo \| _Ctx                      |
| B. 模块   | Module | **闭集?§3.2**（`_Def` `_Types` **`_Ops`** …；**?* TYPE 四型后缀?*?*再新?`MODULE …_Algo`?|
| C. 变量   | 变量   | *_desc\| *_state \| *_algo \| *_ctx                  |
| D. 子程?| 过程   | 导出四段式全名；模块内可短名                         |

---

## 九、逐层逐域扫描与命名映射表

### 9.1 产出物与顺序

在完?*层级-域级-功能集（及次级子模块?*架构图与目录表后，按下列顺序产出，用于重命名与质量验收：

1. **变量清单**：逐层??逐域??逐模块，列出模块级（全局）变量与典型局部变量（可按抽样或全量）?2. **子程?子函?过程清单**：同上，列出各模块中 `SUBROUTINE`/`FUNCTION` ?type-bound procedure 名称、所在模块、导出与否?3. **命名规范前后映射?*：对需重命名的标识符，列出「当前名 ?规范名」对照，作为重命名脚本或人工修改的依据?
### 9.2 变量清单模板

| 层级   | 域级 | 模块（文件）           | 变量?          | 作用?  | 规范名（若需改） |
| ------ | ---- | ---------------------- | ---------------- | -------- | ---------------- |
| L3_MD  | Mat  | MD_Mat_Domain_Core     | n_materials      | 模块?  | ?               |
| L3_MD  | Mat  | MD_Mat_Domain_Core     | mat_registry     | 模块?  | ?               |
| L4_PH  | Mat  | PH_Mat_Plast_Core      | (局? stress(6) | 局?    | ?               |

说明：模块级变量长度?0，与类型四型或语义一致；局部优?FEA 符号（见 [UFC_命名规范与接口标准 v2.0（REPORTS）](../REPORTS/UFC_命名规范与接口标准_v2.0.md) 1.3）?
### 9.3 子程?函数/过程清单模板

| 层级   | 域级 | 模块（文件）       | 过程名（当前?  | 类型   | 规范名（若需改）     |
| ------ | ---- | ------------------ | ---------------- | ------ | -------------------- |
| L3_MD  | Mat  | MD_Mat_Reg         | MatLib_Init      | SUB    | MD_Mat_Reg_MatLib_Init |
| L3_MD  | Mat  | MD_Mat_Reg         | MatLib_RegisterModel | SUB | MD_Mat_Reg_RegisterModel |
| L4_PH  | Elem | PH_Elem_C3D8_Defn  | PH_Elem_Eval_Stiffness | SUB | ?|

说明：导出过程必须四段式或层?域级-功能_动作；模块内实现可短名，但对外别名须符合规范。长度≤28?
### 9.4 命名规范前后映射表模?
| 类别   | 当前?              | 规范?                    | 所在位置（层级/?模块?| 备注     |
| ------ | -------------------- | --------------------------- | ------------------------- | -------- |
| Module | MD_MatLib_Plast_Special2 | MD_Mat_Plast_Special2 ?MD_Mat_Plast_Special | L3_MD/Mat | ?Lib 并入域级 |
| SUB    | MatLib_Init          | MD_Mat_Reg_MatLib_Init      | L3_MD/Mat/MD_Mat_Reg       | 导出全名 |
| TYPE   | LoadDef              | MD_Ldbc_LoadDef 或保?文档 | L3_MD/LoadBC/BC              | 与四型一?|
| 变量   | material_id          | mat_id                      | 局?                     | 简写表   |

说明：映射表按「单文件 ?单域 ?全库」分阶段验收；与 [UFC_命名规范与接口标准 v2.0（REPORTS）](../REPORTS/UFC_命名规范与接口标准_v2.0.md) 第四章质量验收标准一致?
### 9.5 扫描方式与脚?
- **推荐脚本**：`UFC/ufc_core/tools/scan_naming_inventory.py`  
  - **输入**：`UFC/ufc_core` 目录树（可指?`--ufc-core DIR`）? 
  - **输出**：在指定目录（默?`ufc_core/docs/naming_reports`，可指定 `--out DIR`）下生成三份 CSV? 
    - `变量清单.csv`：层? 域级, 模块（文件）, 变量? 作用? 规范名（若需改）  
    - `过程清单.csv`：层? 域级, 模块（文件）, 过程名（当前? 类型, 规范名（若需改）  
    - `命名规范前后映射表模?csv`：类? 当前? 规范? 所在位置（层级/?模块? 备注  
  - **用法示例**（在仓库根或任意目录）：  
    `python UFC/ufc_core/tools/scan_naming_inventory.py --ufc-core <ufc_core路径> --out <输出目录>`  
    例如输出?`PLAN/naming_reports`? 
    `python UFC/ufc_core/tools/scan_naming_inventory.py --out PLAN/naming_reports`  
  - 脚本会排?`ExternalLibs`、`external`、`extern` 子目录；层级与域级从相对路径自动推断（如 `L3_MD/Mat/…` ?层级 L3_MD、域?Mat）? 
  - 生成后由人工或后续脚本填「规范名」列，再按单文件→单域→全库分阶段验收? 
- **其他工具**：可复用或扩?`UFC/ufc_core/tools/check_naming_compliance.py`、`scripts/auto_checklist_assistant.py` 做命名合规检查；与扫描脚本配合使用? 
- **排除**：ExternalLibs 不强制纳入扫描与映射；仅 ufc_core 侧接口与封装需符合规范?
### 9.6 命名规范执行策略（持续任务）

- **长期任务定义**：逐层级、逐域级、逐模块的命名扫描与映射表建设是一?*持续进行的治理任?*，而不是一次性活动。每完成一批重?功能开发，都应增量更新变量清单、过程清单与映射表?- **与任务流的关?*：在所有开发任务（新功能、重构、缺陷修复）中，命名规范校验是默认开启的横切检查项；若命名与本规范冲突，应优先修正命名，再继续后续实现?- **自动化防篡改**：推荐在 CI 中集?`check_naming_standard.py` 及命名扫?报告脚本（如 `scan_naming_inventory.py` 或后?`generate_naming_report.py`），对新?修改?`.f90` 强制做命名规范检查，防止“新代码倒回旧风格”?- **扫描策略**：全库治理可按“单文件 ?单域 ?单层??全库”四阶段推进，每一阶段都通过 9.2?.4 的模板产出清单与映射表，并在评审时一并验收?- **ExternalLibs 例外**：ExternalLibs 目录及第三方库源码默认不纳入命名重构，只要求 ufc_core 侧的封装模块按本规范执行，避免不必要的外部代码分叉?
### 9.7 实施前可选：示例域与域级版图

- **示例域先?*：实施全库重命名前，可先?*一个具体域**（如 `L4_PH/Mat` ?`L3_MD/Mat`）做试点：用 `scan_naming_inventory.py` 导出该域变量清单、过程清单与映射表模板，人工填一份「当前名 ?规范名」示例，作为全库模板与评审参考。该示例可单独归档于 `PLAN/naming_reports` 或附于本规范附录?- **是否先重构域级版?*：若当前目录?1.2 中?0 ?ABAQUS 核心域名 + 层级-域级-功能集”不一致，应先完成**域级版图定稿**（轻量收?⚠️ 域、写清域?Charter、四链校验、目标版图草图），再?9.1?.4 产出清单与映射表并执行重命名。可执行流程与域级收敛见 [UFC_层级域级实质性改造计划.md](archive_20260418/PPLAN_过程稿/域分级重构/UFC_层级域级实质性改造计划.md)（原「域级版图定稿方案」未随仓库；**不得在域级未统一前对模块做大规模重命?*（与 1.2 一致））?
---

*版本?.3 | 合并?命名契约 + 简写规范；新增域级划分标准、三四级命名、功能集正反例与次级角色白名单?0 个核心域名、三层结论摘要与 P0 横切?.7 示例域与域级版图?
