# `<Domain>` 设计意图（模板）

> 将本文件复制为 `INTENT.md`，替换尖括号占位；与 `ufc_core` 域内 `CONTRACT.md` 保持一致口径。  
> **机器可读清单**：同目录放置 `manifest.json`（模式见 [`manifest.schema.json`](manifest.schema.json)），由 `UFC/tools/domain_procedure_registry_align.py` 与 `ufc_core` / `generated/` 对账。

## 1. 问题与目标

- **要解决的问题**：
- **本域职责边界**（做 / 不做）：
- **上游 / 下游**（层间数据从哪来、到哪去）：

## 2. 四型（Desc / State / Algo / Ctx）

| 四型 | 类型名（目标） | 常驻位置 | 说明 |
|------|----------------|----------|------|
| Desc | | | |
| State | | | |
| Algo | | | |
| Ctx | | | |

## 3. 核心 `TYPE` 与字段（目标）

### `<TypeName>`

| 字段 | 类型（逻辑） | 说明 |
|------|--------------|------|
| | | |

## 4. 核心过程（目标命名）

| 意图 | 过程名（建议） | TBP / 模块过程 | 备注 |
|------|----------------|----------------|------|
| Init | | | |
| … | | | |

## 5. 与现状差距（维护时填写）

| 条目 | `generated/` / 源码现状 | 目标（本 INTENT） | 优先级 |
|------|---------------------------|-------------------|--------|
| | | | |

## 6. 参考

- 域合同：`ufc_core/.../CONTRACT.md`
- PPLAN / 架构：`docs/05_Project_Planning/PPLAN/...`
